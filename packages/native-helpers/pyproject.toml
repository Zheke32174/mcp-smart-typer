[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mcp-smart-typer-native-helpers"
version = "2.0.0"
description = "Python gRPC service for Windows UI automation"
authors = [
    {name = "MCP Smart Typer Team", email = "team@mcp-smart-typer.dev"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "grpcio>=1.59.0",
    "grpcio-tools>=1.59.0",
    "pyautogui>=0.9.54",
    "pygetwindow>=0.0.9",
    "pynput>=1.7.6",
    "pillow>=10.0.0",
    "protobuf>=4.21.0",
    "opencv-python>=4.8.0",
    "numpy>=1.24.0",
    "pywinauto>=0.6.8",
    "uiautomation>=2.0.18",
    "PyInstaller>=6.0.0",
]
keywords = ["mcp", "ui-automation", "windows", "grpc", "pyautogui"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: Microsoft :: Windows",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: System :: Human Machine Interfaces",
]

[project.urls]
Homepage = "https://github.com/mcp-smart-typer/mcp-smart-typer"
Repository = "https://github.com/mcp-smart-typer/mcp-smart-typer.git"
Issues = "https://github.com/mcp-smart-typer/mcp-smart-typer/issues"

[project.scripts]
mcp-native-helpers = "src.main:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]

[tool.black]
line-length = 100
target-version = ['py38']

[tool.isort]
profile = "black"
line_length = 100

[tool.mypy]
python_version = "3.8"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
